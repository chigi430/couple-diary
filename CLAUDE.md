# CLAUDE.md — 프로젝트 인수인계 문서

> 이 파일은 Claude Code가 프로젝트 맥락을 파악하기 위해 매번 읽는 문서입니다.
> 새 작업을 시작할 때 이 내용을 먼저 참고하세요.

## 프로젝트 개요

- **이름**: 오늘의 우리 (커플 다이어리 앱)
- **한 줄 설명**: 커플이 함께 쓰는 다이어리 앱. 캘린더에 그날의 사진과 기록을 남기는 형태 (타임트리 + 사진/추억 기록).
- **목적**: 여자친구와 함께 쓸 용도로 시작. 부수입(소규모 용돈벌이 수준)도 염두에 둔 사이드 프로젝트.
- **개발자 수준**: 코딩 경험 어느 정도 있음 (완전 초보는 아니지만, 친절한 설명을 선호).

## 기술 스택

- **빌드/프론트엔드**: Vite + React
- **백엔드/데이터**: Supabase (인증, DB, 스토리지, 실시간 기능)
- **개발 환경**: Windows
- **배포 계획**: GitHub → Vercel

## 핵심 기능 (구상/구현)

- 캘린더 UI (날짜별 진입)
- 날짜별 사진 업로드 및 기록 작성
- 커플 간 공유 (두 사람이 같은 다이어리를 함께 봄)
- (추후) 추억 모아보기 / 타임라인

## 현재 상태

- 로그인/회원가입, 초대코드로 커플 연결, 캘린더 기록, 사진 업로드(클라이언트 리사이즈·압축 후 비공개 버킷 저장 + 서명 URL 조회), 실시간 공유까지 Supabase 연동 구현 완료 (README 기준).
- 아직 git 저장소로 초기화되지 않은 로컬 상태 — GitHub 업로드 전 단계.
- 주요 파일: `src/App.jsx`, `src/Auth.jsx`, `src/CoupleGate.jsx`, `src/Calendar.jsx`, `src/Today.jsx`, `src/SignedImage.jsx`, `src/supabaseClient.js`, `src/constants.js`, `src/utils.js`, `src/styles.js`

## 앞으로 할 일 (TODO)

- GitHub 저장소 생성 및 초기 커밋/푸시
- Vercel 연결 및 배포 (환경변수 `VITE_SUPABASE_URL`, `VITE_SUPABASE_KEY` 등록 필요)
- 공휴일 데이터(`src/constants.js`, 현재 2026년 예시)를 공공데이터포털 API로 자동화할지 검토

## 작업 시 지켜줄 것

- 코드를 수정하기 전에 무엇을 왜 바꾸는지 먼저 설명할 것.
- 큰 변경은 한 번에 하지 말고 단계별로 진행하고, 각 단계마다 확인받을 것.
- Supabase 키 등 민감한 값은 절대 코드에 하드코딩하지 말고 환경변수(.env)로 관리할 것.
- 설명은 한국어로, 초보 친화적으로.

## 환경변수 (참고)

- Supabase URL / anon key 등은 `.env` 파일에 두고, `.gitignore`에 반드시 포함할 것.
